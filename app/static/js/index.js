//import React from "react"

class Index extends React.Component{
    render(){
        alert("Bitch");
        return <header>
                    <a href="#">Ok then</a>
                </header>
    }
}
ReadctDOM.render(React.createElement(Index), document.getElementById("root"));
